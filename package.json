{
  "name": "consistent-env",
  "version": "1.3.1",
  "description": "A reliable way to get env",
  "main": "lib/index.js",
  "scripts": {
    "test": "(apm test) && (flow check) && (eslint . )",
    "clean": "rm -rf lib",
    "compile": "npm run clean && babel src --out-dir lib",
    "watch": "npm run clean && babel src --out-dir lib --watch"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/steelbrain/consistent-env.git"
  },
  "author": "steelbrain",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/steelbrain/consistent-env/issues"
  },
  "homepage": "https://github.com/steelbrain/consistent-env",
  "keywords": [
    "environment",
    "env"
  ],
  "devDependencies": {
    "babel-cli": "^6.11.4",
    "babel-preset-steelbrain": "^5.0.0",
    "eslint-config-steelbrain": "^3.0.0",
    "flow-bin": "^0.46.0",
    "jasmine-fix": "^1.0.1"
  },
  "dependencies": {
    "lodash.uniq": "^4.5.0"
  }
}
